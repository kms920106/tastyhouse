plugins {
    id 'java-library'
    id 'idea'
}

dependencies {
    runtimeOnly project(':infrastructure:external')
    runtimeOnly project(':infrastructure:firebase')
}

jar {
    enabled = true
    archiveClassifier = ''
}

bootJar {
    enabled = false
}
